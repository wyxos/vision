import Command from "../Command.mjs";

export default class ReleaseNodePackage extends Command {
    signature = "release:node";
    description = "A sample description of the command.";

    handle() {
        // Command logic here
    }
}
