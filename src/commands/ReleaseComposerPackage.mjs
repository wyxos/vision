import Command from "../Command.mjs";

export default class ReleaseComposerPackage extends Command {
    signature = "release:composer";
    description = "A sample description of the command.";

    handle() {
        // Command logic here
    }
}
